# Client Requirements — AI Facial Analysis Platform

## Document Control

| Field | Value |
|---|---|
| Document name | `client_requirements` |
| Role | **Single source of truth** for the entire project. All other project documents (`PRD.md`, `BRD.md`, `ARCHITECTURE.md`, `TECHNICAL_DESIGN.md`, `API_SPECIFICATION.md`, `DATABASE_DESIGN.md`, `UI_UX_REQUIREMENTS.md`, `AUTHENTICATION.md`, `SECURITY.md`, `TESTING.md`, `DEPLOYMENT.md`, and any future specialized doc) must be derived from and validated against this document, not the other way around. |
| Version | 1.21 |
| Status | Draft — active project, expected to evolve |
| Last updated | 2026-09-10 |
| Supersedes | v1.20 of this document. v1.20 superseded v1.19, which superseded v1.18, which superseded v1.17, which superseded v1.16, which superseded v1.15, which superseded v1.14, which superseded v1.13, which superseded v1.12, which superseded v1.11, which superseded v1.10, which superseded v1.9, which superseded v1.8, which superseded v1.7, which superseded v1.6, which superseded v1.5, which superseded v1.4, which superseded v1.3, which superseded v1.2, which superseded v1.1, which superseded v1.0, which superseded the two earlier "Developer Project Overview" drafts. |
| Sources this document is built from | `Discussion.docx`, `AI Facial Analysis_Proposal.docx`, `AI Facial Analysis Proposal.docx.pdf`, `Estimation.xlsx`, `protocol_report (3).pdf`, `Qoves Onboarding.mp4`, `Screenshot 2026-06-12 at 10.51.25.png` (original requirement materials) + direct client decisions given in conversation on 2026-09-07 (Phase 1 scope decisions, custom-authentication + Redux + documentation-hierarchy requirements, the v1.1 answers resolving most of v1.0's open questions, the v1.2 tech-stack revision, and the v1.3 architecture-fork resolution) + **v1.4 session-persistence / httpOnly refresh-cookie revision** (delivery-team, aligning docs with the implemented auth module and the requirement that sessions survive refresh/tab/browser restart until explicit logout or an invalid refresh session) + **v1.5 forgot-password documentation + reset-flow redesign, and UI-convention additions** (delivery-team; backfills three previously-undocumented-in-this-file auth additions and records the reset-password screen split) + **v1.6 onboarding-questionnaire content** (client-sourced, from `Qoves Onboarding.mp4` — see `docs/onboarding_questionnaire_spec.md`; resolves the previously-blocking open item for `FR-003`/`FR-004`) + **v1.7 photo-capture flow pointer** (delivery-team, from `docs/photo_capture_spec.md`; resolves `OQ-008`'s threshold deferral into an implemented six-check gate and records a new not-yet-client-confirmed assumption, `ASM-005`) + **v1.8 facial-analysis-engine implementation** (delivery-team; implements `FR-007`/`FR-008` and records a new not-yet-client-confirmed AI-vendor substitution, `ASM-006`) + **v1.9 report-generation implementation** (delivery-team; implements `FR-009`–`FR-014` and records a new not-yet-client-confirmed recommendation-tiering assumption, `ASM-007`) + **v1.10 payment implementation, first pass** (delivery-team; implements `FR-015`/`FR-016` with a deferred/two-stage analysis pipeline, since superseded by v1.11) + **v1.11 payment implementation, pay-before-analysis** (delivery-team; the user directly instructed a stricter model — no analysis of any kind runs before payment succeeds — superseding v1.10's hybrid; revises `ASM-008` accordingly) + **v1.12 user-dashboard implementation** (delivery-team; implements `FR-017` and records a new, not-requiring-sign-off scope clarification, `ASM-009`) + **v1.13 dashboard/profile/landing revisions** (user-directed: adds `full_name` at signup, an in-app password-change endpoint, a persistent dashboard nav button, and an animated landing-page "How it works" section; revises `ASM-009`) + **v1.14 narrative-prompt content-quality refinement** (delivery-team; improves `FR-008`'s AI narrative call's inputs/instructions — labeled questionnaire context, measurement citation, personalization — no requirement text change) + **v1.15 cross-photo identity check + angle-aware ear measurement** (user-directed bug fix: BR-005 gains a set-level "same person in all three angles" check, enforced both in the UI and server-side; FR-007's Ears measurement now uses the left/right 3/4-angle photos instead of the front photo, which showed ears poorly; records `ASM-010`) + **v1.16 identity-check guard-chain bypass fix** (user-reported follow-up: retaking a flagged photo appeared "stuck" because entry routing let a mismatched-but-individually-passed photo set sail past `/photos` from any entry point, not just a direct revisit) + **v1.17 odd-one-out mis-identification fix** (user-reported follow-up: the identity check sometimes named the wrong photo as the mismatch, because it picked the outlier by largest summed pairwise distance rather than a deterministic match-count vote; replaced per the user's own explicit algorithm — front treated as protected reference whenever the vote is ambiguous) + **v1.18 "all three mutually different" now flags both non-front photos** (user-reported follow-up: when all three photos are different people, only the right-side photo was ever named regardless of which side was actually swapped, because the v1.17 tie-break picked a single "farther from front" photo using a distance comparison that reflected pose geometry, not identity; front is kept as the reference and both other photos are now flagged together — `mismatched_angles: list[str]` replaces the singular `odd_one_out` field end-to-end) + **v1.19 identity-validation design contract + audit** (user-supplied a full written spec of the expected stateless-recompute-on-every-retake behavior; audited the implementation clause by clause, found it already conformant since v1.18, and formalized the contract as `docs/photo_identity_validation_spec.md` plus named regression tests tracing back to the spec's own worked examples — no behavior change) + **v1.20 auto-redirect guard race fix** (user reported the v1.19 audit was wrong in practice — a retake could get the user auto-navigated to `/payment` before the fresh comparison was even checked; found and fixed a stale-state bug the v1.19 audit missed, in `usePhotoUploadGuard.ts`'s redirect effect, which read the live `identityCheck` value including its transient post-retake `null` state as "safe to redirect") + **v1.21 PhotoSetCompleteStep.tsx field-name regression fix + live 3-identity verification** (found `PhotoSetCompleteStep.tsx` had reverted to the pre-v1.18 singular `odd_one_out` field — a real `tsc` compile error against the current `mismatched_angles: list[str]` API shape, silently masking all per-photo error highlighting in dev mode; re-applied the v1.18 fix, then live-verified the user's exact "front/right/left all different people, fix front, then fix right" sequence end-to-end with three genuinely distinct synthesized identities, confirming the error set narrows correctly at each step). |

### Change log
- **v1.21 (2026-09-10):** Two follow-ups. First, the user asked to check `PhotoSetCompleteStep.tsx` directly — it had reverted to referencing `identityCheck.odd_one_out`, a field the `IdentityCheck` type no longer has (`mismatched_angles: string[]` since v1.18); confirmed via `tsc --noEmit` as a real compile error (`TS2339`). Next.js's dev server doesn't block on type errors, so this ran silently: `odd_one_out` evaluated to `undefined` at runtime, which meant no per-photo card ever highlighted as flagged (all rendered green) even while the top banner and message text correctly described a mismatch. Re-applied the `mismatchedAngles` logic (multi-angle highlighting, pluralized copy, `disabled={mismatchedAngles.length > 0}`); `tsc`/`eslint`/`next build` all clean afterward, and confirmed no other file had similarly reverted (repo-wide grep for `odd_one_out`/`oddOneOut` came back empty elsewhere). Second, the user described the exact symptom this would produce — *"i change front to c so basically error i should got only in right which is b not in right and left both... even then i change right to c then also error stay in right and left both"* — and it was left unverified whether this was purely the field-name bug or something deeper. Rather than assume, synthesized three genuinely distinct identities (real fixtures only had two base identities; a third, `third_person_front.jpg` and its pose variants, was constructed via piecewise horizontal image warping and verified via direct signature-distance computation to be properly separated from both existing ones — over threshold from both, not just from one) and live-tested the user's exact sequence in Chrome: front=A/right=B/left=C (all different) → both right+left correctly flagged; retook front→C → right-only correctly flagged, left correctly cleared; retook right→C → all valid, Continue enabled. Confirms the v1.21 field-name fix (not a separate deeper bug) was the actual cause of what was reported. Scratch-only verification fixtures removed after use (not added to the permanent test suite, unlike the earlier `different_person_right_3q.jpg`/`different_person_left_3q.jpg`, which are referenced by real tests).
- **v1.20 (2026-09-10):** The user pushed back hard on the v1.19 audit's conclusion — *"No its not working similar to requirements, if 2 error it allow to 2 retake any 2 and automatically continue... why it is not happening like that"* — reporting that a retake could get them auto-advanced to `/payment` without the real comparison happening. This was correct, and the v1.19 audit had missed it: it traced `check_photo_set_identity()`/`_determine_mismatched_angles()` and the store's replace-not-merge behavior thoroughly, but never inspected `frontend/src/hooks/usePhotoUploadGuard.ts`'s own auto-redirect effect as a *consumer* of `identityCheck`. That effect watched the live `identityCheck` value and skipped the review screen once it looked consistent — but `photoStore.setAnglePhoto()` (fired the instant any retake's upload response returns, before the fresh cross-photo re-check even starts) resets `identityCheck` to `null` first, and the effect's guard (`if (identityCheck?.consistent === false) return`) didn't treat `null` as unsafe (`null?.consistent` is `undefined`, not `false`) — so it fell through and redirected immediately, hardcoding `photosIdentityConsistent: true`, regardless of whether the retake actually fixed anything. In practice this fired on nearly every retake once a set was already complete when the page was entered (the normal case for a returning user). Fixed by deciding the "already done, skip the screen" redirect exactly once, from the initial page-load fetch's own result only (a new `wasConsistentOnFetch` ref, mirroring the existing `wasCompletedOnFetch` pattern) — never from the live, retake-mutable store value; a retake can no longer re-trigger this effect at all, only a fresh page load/reload re-evaluates it. Live-verified in Chrome: a retake that changed *which* angle was wrong (not fixed) no longer redirects and shows the new error correctly; reaching genuine consistency via retakes stays on the page with Continue enabled until manually clicked (no more automatic navigation); and a mismatch introduced entirely outside the browser session is picked up correctly on a fresh page load. Updated `docs/photo_identity_validation_spec.md` (new §6, "The one place this WAS broken") with the full root-cause writeup, since this is exactly the kind of gap that document exists to prevent recurring. No backend change; no automated regression test exists for this (frontend state-timing bug, outside this project's Python test suite, and there is no frontend test harness — `tsc`/`eslint`/live-Chrome-verification is this codebase's established frontend testing convention).
- **v1.19 (2026-09-10):** The user supplied a complete written specification of the expected identity-validation behavior — stateless recompute on every retake, errors displayed only on the currently-mismatched image(s), the matching group re-derived from the current three photos rather than anchored to a fixed "front is always right" assumption — with explicit worked scenarios (sequential retakes, and two "the error moves to an untouched angle" flip-flop cases). Rather than assume the v1.18 fix already covered this, audited the actual code against every clause: `check_photo_set_identity()` and `_determine_mismatched_angles()` are pure functions with no persisted state; `GET /photos/status` recomputes fully on every call; `photoStore.setStatus()` replaces `identityCheck` wholesale (never merges) and `setAnglePhoto()` resets it to `null` on every optimistic local update; `PhotoSetCompleteStep.tsx` derives its error display straight from the current `identityCheck` prop with no local memory. Traced all of the user's worked examples through the code by hand — every one already produces the expected result (this was the direct effect of the v1.18 fix, one turn earlier). No functional code changed. Formalized the algorithm and state-management contract as a new document, `docs/photo_identity_validation_spec.md` (mental-model diagram, ownership table, the group-matching priority rules, and the worked-example table), and added `TestExactUserReportedScenarios` to `Backend/tests/unit/test_photo_validation_service.py` — the user's exact scenarios encoded as regression tests, named after the spec's own scene numbering, so a future change that breaks one of these traces directly back to the spec clause it violates. 245 backend tests passing (up from 238).
- **v1.18 (2026-09-10):** Third follow-up to `ASM-010` (v1.15–v1.17) — after v1.17's match-count-vote fix, the user re-tested with all three photos as genuinely different people and reported the error always named the right-side photo regardless of which side was actually the problem ("instead of giving error in right and left it is giving error in right in all cases"). Root cause: the "all three mutually mismatch" branch of `_determine_odd_one_out()` picked a single winner between the two non-front photos by comparing which was farther from front — a comparison with no real identity meaning once all three are already established as different people, since front-to-right and front-to-left baseline distances differ for pose-geometry reasons, not identity ones. Per the user's own explicit specification (front is the kept/main reference in this case), fixed by returning **both** non-front photos as mismatched instead of arbitrarily picking one. This required changing the API shape: `IdentityCheckResult`/`IdentityCheckOut`/`IdentityCheck` (frontend) all replace the singular `odd_one_out: string | null` with `mismatched_angles: list[str]` (empty when consistent), and `PhotoSetCompleteStep.tsx` now highlights every flagged card and blocks Continue while the list is non-empty (previously only ever a single card). The renamed helper `_determine_mismatched_angles()` in `Backend/app/services/photo_validation_service.py` keeps the v1.17 match-count logic for the common single-mismatch case unchanged (re-verified live for all three individual positions — front, right, left — including synthesized angle-appropriate mismatch fixtures for right_3q/left_3q, since the existing `different_person.jpg` fixture only passes its own pose_match check for the front angle). Also live-verified: a fully consistent, complete set that gets one photo retaken into a mismatch immediately shows the error without a reload, and retaking it back to a matching photo clears it again, entirely within one continuous session; and photo/blob rows in the database update in place on retake (no duplicates) across all tested scenarios. The one case not reproducible live (no third distinct real-person fixture exists in the repo) — "all three mutually different" flagging both non-front photos — is covered instead by direct unit tests against synthetic pairwise distances. No requirement text change; new integration tests also cover right_3q/left_3q mismatches through the real upload endpoint (previously only front, since `different_person.jpg` alone only passes `pose_match` for that angle — two new angle-appropriate fixtures, `different_person_right_3q.jpg`/`different_person_left_3q.jpg`, close that gap). 238 backend tests passing (up from 234).
- **v1.17 (2026-09-10):** Second follow-up to `ASM-010` (v1.15/v1.16) — the user reported that the identity check correctly detected a mismatch but named the **wrong** photo (uploading a different person as the left-side photo produced an error pointing at the right side instead), so retaking the flagged photo never cleared the error since it was never the actual problem. Root cause: `check_photo_set_identity()`'s odd-one-out selection picked the photo with the **largest summed pairwise distance** to the other two, which is sensitive to pose-driven baseline noise — front-to-right and front-to-left distances aren't perfectly symmetric even for the same person, and this could occasionally tip the sum toward the wrong photo. Replaced with the user's own explicit algorithm: a deterministic match-count vote (each photo counts how many of the other two it's within the identity threshold of; the one matching fewest is the odd one out), with `front` treated as a protected reference whenever the vote ties — either because all three photos are mutually inconsistent (front is never flagged in that case) or because a "hub" photo matches both others while they don't match each other. New `_determine_odd_one_out()` helper in `Backend/app/services/photo_validation_service.py`, independently unit-tested against synthetic pairwise distances for both tie cases (not reproducible from the repo's one `different_person.jpg` fixture alone), plus a new real-fixture test for the exact left-side-mismatch scenario reported. No API/schema/frontend change — `IdentityCheckResult`'s shape is unchanged, and the retake→recheck→clear-error pipeline (`PhotoWizard.tsx`/`PhotoSetCompleteStep.tsx`) was re-verified correct and untouched. 234 backend tests passing (up from 227).
- **v1.16 (2026-09-10):** Follow-up to `ASM-010` (v1.15) — the user reported that after retaking the flagged photo, the error appeared stuck / not updating. The retake mechanism itself was correct (verified via a direct reproduction test hitting the real endpoints in sequence); the actual bug was in routing. `lib/onboarding/nextStep.ts`'s `getNextOnboardingStep` — the single shared function every guard/entry-point uses to decide "where does this user belong right now" — only ever checked `photosCompleted` (every angle individually passed BR-005's per-photo checks), never whether the set was cross-photo identity-consistent. A mismatched set is still `photosCompleted: true` by design (the mismatch is a set-level signal, not a per-photo failure), so **any** entry point that computed the next step — logging in, visiting `/dashboard` directly, finishing the questionnaire — routed a mismatched user straight past `/photos` to `/payment`, skipping the one screen that shows and lets them fix it. Reproduced live: a freshly logged-in user with a mismatched set landed on the payment paywall with no indication anything was wrong. Fixed by adding a `photosIdentityConsistent` parameter to `getNextOnboardingStep` and threading the real value through from `photoStore`'s `identityCheck` at all six call sites (`useOnboardingEntryGuard`, `usePhotoUploadGuard`, `usePaymentGuard`, `useQuestionnaireGuard`, `PhotoWizard.tsx`, `QuestionnaireWizard.tsx`). Verified live end-to-end in a real browser session after the fix: a mismatched set now correctly holds the user on `/photos` with the error shown from every entry point, and retaking the flagged photo correctly clears it and allows the user to proceed. No backend change needed (`PhotoIdentityMismatchError` at checkout/analysis already caught this server-side — this was purely a frontend UX gap that let a user get an unhelpful error later instead of a fixable one at the right screen).
- **v1.15 (2026-09-10):** Two user-reported issues fixed, both in the photo-upload/analysis pipeline: (1) **Cross-photo identity check (`BR-005`).** Per-photo validation never checked whether the front/left/right photos were actually of the same person — a user could (and, per direct report, did) upload a different person's photo for one angle and have it silently accepted. `Backend/app/services/photo_validation_service.py` gained `check_photo_set_identity`, run once all three required angles have individually passed (not blocking any single upload): it computes a small landmark-ratio "signature" per photo (MediaPipe Face Landmarker, the same model `facial_measurement_service.py` uses) and flags the photo whose signature doesn't match the other two. Surfaced on the "all photos uploaded" review screen (`PhotoSetCompleteStep.tsx`) — the mismatched photo is highlighted, Continue is blocked until it's retaken — and independently re-checked server-side at both `POST /payments/checkout` and `POST /analysis` (`PhotoIdentityMismatchError`, 409) so the UI gate can't be bypassed by calling either endpoint directly, same posture as every other payment/analysis guard in this codebase. Threshold calibrated empirically (not assumed) against this project's own same-person fixtures and a genuinely different person's photo — see `ASM-010`. (2) **Ears measurement was silently front-photo-only (`FR-007`).** `extract_measurements` always ran on the front photo alone; for most of the 7 mesh-based features that's the *correct* choice (a 3/4 turn foreshortens symmetry comparisons, verified empirically), but Ears is the one feature a front-facing photo genuinely shows poorly. `_measure_ears` now uses the left_3q/right_3q photos (each shows its own side's ear clearly), falling back to front only if a side photo is missing. Records **`ASM-010`**: both the identity-check signature/threshold and the ear-measurement formula are first-pass heuristics, same "reasonable default, expect a tuning pass" posture as `ASM-002`. 227 backend tests passing (up from 211), including new bypass-attempt tests for both new server-side guards.
- **v1.14 (2026-09-09):** Content-quality refinement of `FR-008`'s AI narrative call (`Backend/app/services/ai_narrative_service.py`) — no report structure/schema change, per direct user request ("current analysis and version is perfect... just enhance it, improve prompt"). Two gaps fixed: (1) the questionnaire context sent to the AI was a raw, unlabeled `{"q4": "Masculine"}`-style JSON dump — the AI had no way to know what each opaque question id actually asked; now each answered question is paired with its real text (`questionnaire_service.QUESTIONS_BY_ID`) before being sent. (2) The narrative instruction now requires citing an actual measurement value in prose when available (not just a vague reference to "the measurements"), narrative length guidance increased slightly (2-4 → 3-5 sentences), and the prompt now explicitly instructs the AI to personalize tone using the user's stated goal/motivation/liked-or-disliked feature, to use an especially measured and reassuring tone when the questionnaire indicates elevated appearance-related distress (tied to `FR-004`'s BDD-awareness intent), and to never reference medical/medication/allergy answers in cosmetic commentary. `report_assembly_service.py`, `report_pdf_service.py`, `schemas/reports.py`, and every frontend report file are unchanged — same JSON keys in, same JSON keys out. New `Backend/tests/unit/test_ai_narrative_service.py` unit-tests the prompt-building functions directly (no real API call, `BR-006`). 211 backend tests passing (up from 201).
- **v1.13 (2026-09-09):** Four user-directed fixes/revisions to the just-shipped `user-dashboard` module (`FR-017`) and the landing page (`FR-001`): (1) a persistent "Dashboard" nav button in the app header (`(protected)/layout.tsx`), alongside the existing report/analysis flow, which is otherwise unchanged; (2) `POST /auth/change-password` replaces the sign-out-and-redirect-to-forgot-password profile flow — no session revocation, current/new/confirm password fields inline on the dashboard's Profile card; (3) `full_name` added to `DATA-001`, required at signup, used for the dashboard/menu greeting instead of an email-derived guess; (4) the landing page gained a header "Sign in" link and an animated, scroll-triggered "How it works" section (questionnaire → photos → analysis → report). Revises **`ASM-009`** (see §11) — items (2) and (3) directly override that entry's original "view-only, sign-out-based" design. See `docs/api-specification.md` §2/§3, `docs/database-design.md` §2.1.
- **v1.12 (2026-09-09):** Implements `user-dashboard` (`FR-017`) — `/dashboard` composes `GET /users/me` (new), `GET /reports`, and `GET /payments` into report status/download, payment history, and a profile section. Records **`ASM-009`**: profile management is view-only (no editable field exists on the User entity) — see below. **Revised in v1.13** — see above.
- **v1.11 (2026-09-09):** Supersedes v1.10's payment design with a stricter one, per the user's explicit direct instruction: **payment must complete before analysis starts at all** — not even the free CV/MediaPipe measurement step runs pre-payment. The paywall now appears immediately after photo upload (a new `/payment` screen), before any teaser or partial content exists; `FR-015`'s "a teaser/results page **may** be shown pre-payment" is optional wording, so not showing one is still compliant, just a narrower exercise of that option than v1.10's design used. **Same-day amendment:** the first pass had a Stripe webhook trigger analysis automatically; the user reported this made the "analyzing" step invisible (the pipeline can finish in a couple of seconds) and asked for an explicit "Start Analysis" button back, mirroring the pre-payment-gate UX — reverted, so the webhook now only flips payment status and `POST /analysis` stays a user-facing click, just gated behind payment the same way it always was. "Payment before analysis start" is still exactly true; only the trigger's *automaticity* was walked back, not its gating. Revises **`ASM-008`** (see §11) to describe this stricter gate instead of the superseded deferred-narrative split. `Payment` no longer references a `Report` row (none exists yet at payment time) — it gates the user's account directly. Price stays a configuration value (`REPORT_PRICE_CENTS`/`REPORT_PRICE_CURRENCY`), `OQ-002` remains open. The auto-trigger version was verified end-to-end with real Stripe test-mode Checkout; the reverted version has automated-test and build coverage, with the live walkthrough left to the user's own manual test.
- **v1.10 (2026-09-09):** Implements `payment` (`FR-015`, `FR-016`) — Stripe hosted Checkout + webhook, gating full report content and PDF download behind a succeeded payment, enforced server-side (verified by a direct-API-call bypass-attempt test, `Backend/tests/integration/test_payment_flow.py::TestBR001BypassAttempt`). Records **`ASM-008`**: the AI narrative call (`FR-008`) is now deferred into a paid **Stage 2**, split from a free **Stage 1** (CV measurements only) that keeps `POST /analysis`'s existing trigger timing — an internal refinement of *when* the already-approved `BR-006` AI cost is incurred, not a change to any client-stated requirement, so it needs no client sign-off (unlike `ASM-005`–`ASM-007`). Price stays a configuration value (`REPORT_PRICE_CENTS`/`REPORT_PRICE_CURRENCY`), `OQ-002` remains open. **Superseded by v1.11** — see above.
- **v1.9 (2026-09-08):** Implements `report-generation` (`FR-009`–`FR-014`, 11-section report assembly + auto-publish + branded PDF export). Assembly is a synchronous, **AI-free** data transform of Phase 4's already-computed measurements/narrative — no new AI call, no async/polling endpoint. Records **`ASM-007`**: `FR-012`'s three recommendation tiers (at-home/lifestyle, OTC/skincare-active, in-clinic) are produced by a first-pass keyword heuristic over the existing flat recommendation strings, not a dedicated AI call or a change to Phase 4's prompt — delivery-team decision, **not yet confirmed by the client**, not clinically validated, same posture as `ASM-002`'s photo-validation thresholds. PDF export built with **ReportLab** (pure-Python, no system dependencies), a delivery-team choice not specified by the client. Also resolves `docs/database-design.md`'s previously-open "can a user have multiple reports?" question: **one report per user**, 1:1 with the analysis result — consistent with Phase 4's existing one-analysis-per-user posture.
- **v1.8 (2026-09-08):** Implements `facial-analysis-engine` (`FR-007`/`FR-008`, MediaPipe CV measurement + AI narrative generation). Records **`ASM-006`**: `NFR-008` names OpenAI specifically (client-stated); the delivery team built this against **DeepSeek** instead, since DeepSeek's hosted API is OpenAI-Chat-Completions-compatible (same request shape, swappable back to real OpenAI via config alone) and the delivery team had a DeepSeek key available for development. **Not yet confirmed by the client** — flag alongside `ASM-005`/`OQ-002` at the next check-in. Also resolves the "sync vs. async analysis endpoint" item `phase-wise-requirements.md` had flagged as this phase's own decision: an in-process background task + polling, not a new task-queue.
- **v1.7 (2026-09-08):** Points `FR-005`/`FR-006`/`BR-005` at the new [`docs/photo_capture_spec.md`](./photo_capture_spec.md) (delivery-team-authored implementation spec, derived from the same Qoves reference material as the questionnaire) and records **`ASM-005`** — that spec's own 3-angle default vs. the client's 7-pose Qoves reference, **not yet confirmed by the client**. (Note: `photo_capture_spec.md` labels this same decision "`ASM-004`" using its own internal numbering, chosen before cross-checking against this document's already-taken `ASM-004` — id `ASM-005` here is this document's canonical reference to that same open decision; do not conflate the two numberings.) `OQ-008` (exact photo-validation thresholds, previously "resolved by deferral") is now genuinely resolved — implemented as a six-check gate in `Backend/app/services/photo_validation_service.py`, thresholds recorded in `docs/security.md` §6.
- **v1.6 (2026-09-08):** Resolves the single biggest open item in the whole documentation set: the literal 23-question onboarding-questionnaire content and branching logic (`FR-003`, `FR-004`), sourced directly from the client's own Qoves reference recording — see the new `docs/onboarding_questionnaire_spec.md`. Unlike v1.5's additions, this is genuinely **client-sourced content**, not a delivery-team decision (two small items in that spec remain `[Assumption]`, explicitly flagged, pending client confirmation: the Q9/Q11 medication/allergy follow-up shape, and Q19's exact branching condition). Implementation of Phase 2 (`landing-page` + `onboarding-questionnaire` modules) proceeds against this content.
- **v1.5 (2026-09-07):** Two kinds of change. (1) **Backfill of undocumented delivery-team additions** that were already in shipped code but never captured here: CSRF protection on cookie-authenticated endpoints (`AUTH-013`), `GET /auth/me` (`AUTH-014`), and the forgot-password/reset-password sub-flow itself (`AUTH-015`) — this document previously had zero mention of password reset despite it existing in the app since the initial auth build. (2) **Reset-flow redesign**: password reset is now three screens (request email → verify code → set new password) instead of one combined OTP+new-password screen/request, using a short-lived, single-use `reset_token` credential minted after OTP verification (see `AUTH-015`, `docs/authentication.md` §2.5). Also records two new standing UI conventions applying to every module going forward, not just auth: toast notifications (top-right, mandatory success/error feedback on every user-initiated action — see `docs/ui-ux-design.md`) and a shared confirmation dialog required before any destructive/irreversible action (delete, logout, etc.). None of this is client-stated; all labeled `Decided by delivery team` below.
- **v1.4 (2026-09-07):** Session persistence & refresh-token transport revision. Refresh tokens travel as an **httpOnly cookie** set by the backend; only the short-lived access token (+ user) live in the in-memory Zustand store. This supersedes the v1.1–v1.3 reading of `FE-002` / `OQ-009` / `ASM-001` that placed both tokens in the client-side store. Refresh lifetime revised to **7 days** (from 30) under `AUTH-012` to match the inactivity/logout product rule for this cookie model. Access-token expiry, page refresh, tab/window/browser close, network blips, and 5xx must **not** clear auth or redirect to `/login` — only explicit logout or a backend-confirmed invalid/revoked/expired refresh session may. Frontend restores sessions on startup via `POST /auth/refresh` while `status === "idle"` (`isAuthInitializing`); protected routes wait during that window. Proactive access-token refresh (~1 minute before expiry) plus 401→refresh→retry (single-flight) are both required. Still forbidden: `localStorage`, `sessionStorage`, and Zustand `persist` for auth.
- **v1.3 (2026-09-07):** Resolves `ASM-004` (the v1.2 open question of whether frontend and backend consolidate into one Next.js app). Client confirmed: **two separate apps**, not one — a Next.js/TypeScript frontend and a **Python + FastAPI backend**. This partially reverts v1.2: the backend-framework, ORM, and migration-tool changes from v1.2 are superseded back to their v1.1 values (`NFR-001` → Python + FastAPI, `NFR-004` → SQLAlchemy, `NFR-003` → Alembic), because those v1.2 changes were themselves premised on the now-rejected single-app consolidation (SQLAlchemy/Alembic are Python-only and cannot run inside a Next.js app, which is why v1.2 had swapped them for Prisma in the first place — with the consolidation itself reverted, that swap's rationale no longer applies). Everything else from v1.2 stands: `NFR-002` (vendor-neutral PostgreSQL, no Supabase), `NFR-005` (Next.js frontend), `NFR-006`/`FE-001`/`FE-002` (Zustand, not Redux) are unaffected by this revision. The two apps communicate over HTTP/CORS (the frontend calls the backend's REST API directly from the browser), replacing the ambiguous "layering lives inside one app" framing v1.2 had assumed.
- **v1.2 (2026-09-07):** Client-requested tech-stack revision, applied across the stack requirements only — no business/functional/auth-flow requirement changed. Backend framework changed from Python + FastAPI to TypeScript + Next.js; because Next.js is a full-stack framework, the frontend was consolidated into the same Next.js application rather than remaining a separate Vite + React app — this was a delivery-team interpretation of a brief client instruction ("use typescript and nextjs in backend"), flagged in `ASM-004` for client confirmation. **The backend-framework/consolidation part of this entry is superseded in v1.3 — see above.** Database changed from Supabase-hosted Postgres to a vendor-neutral **PostgreSQL** requirement (`NFR-002`, stands) — Supabase is no longer part of the stack in any role, which also retires `CON-002`'s Supabase-specific wording (generalized instead, see `CON-002`). ORM changed from SQLAlchemy to Prisma (`NFR-004`, **superseded in v1.3, reverted to SQLAlchemy**); migrations changed from Alembic to Prisma Migrate (`NFR-003`, **superseded in v1.3, reverted to Alembic**). Frontend state management changed from Redux/Redux Toolkit to **Zustand** (`NFR-006`, `FE-001`, `FE-002`, stands) per client instruction to use "another [library] for store management according to Next.js" — the same layering contract and Redux-not-`localStorage` token-storage decision (`ASM-001`) carry over unchanged, only the library name changes.
- **v1.1 (2026-09-07):** Resolved 8 of the 9 open questions from v1.0 — see Section 14 for the full resolution table. Key changes: authentication flow is now fully specified (email + password, then OTP, for both signup and login); frontend framework confirmed as React + Vite (**superseded in v1.2**); ORM confirmed as SQLAlchemy (**superseded in v1.2**); OTP delivery channel confirmed as email; refresh-token storage confirmed as the Redux store (**library superseded in v1.2 — see Zustand**, storage location/rationale unchanged); OTP/token lifetime values decided (client delegated this to the delivery team "considering security"); hosting decision explicitly deferred (not needed right now); photo-validation thresholds explicitly deferred to development time. Only report pricing (OQ-002) remains genuinely open, at the client's request.
- **v1.0 (2026-09-07):** Initial source-of-truth document, consolidating all original requirement materials plus the client's Phase 1 scope decisions and custom-authentication/Redux/documentation-hierarchy requirements.

### How to use this document
1. Read and understand this document fully before creating or updating any derived document (`PRD.md`, `ARCHITECTURE.md`, etc.).
2. Do not invent requirements not supported here — anything not explicitly stated by the client is labeled **[Recommendation]**, **[Assumption]**, or **[Decided by delivery team]** below, and must stay labeled that way if it's carried into a derived document.
3. Keep derived documents consistent with this source. If a derived document would conflict with something here, that conflict must be surfaced, not silently resolved.
4. When this document changes, re-check every derived document that references the changed requirement ID(s) (see the Traceability Map, Section 13). No derived documents exist yet as of v1.1 — once created, they should be checked against this change log on every future update.
5. Keep business, product, technical, architecture, security, and implementation concerns in separate sections/documents — this file mixes them only because it is the intentionally comprehensive source; derived documents should not.

### Requirement ID legend
`BC-*` Business Context · `FR-*` Functional Requirement · `AUTH-*` Authentication/Authorization · `FE-*` Frontend requirement · `NFR-*` Non-functional/Technical · `BR-*` Business Rule · `WF-*` Workflow · `DATA-*` Data entity · `CON-*` Constraint · `ASM-*` Assumption/Recommendation (not client-stated) · `OQ-*` Open question

---

## 1. Business Context (`BC-*`)

- **BC-001** [Client-stated] The product is an AI-driven facial aesthetics analysis platform, conceptually similar to Qoves.com — an existing real-world service that produces expert, cephalometric-based "aesthetic protocol" reports for clients.
- **BC-002** [Client-stated] The business problem being solved: Qoves-style services depend on human experts manually producing each report, which is slow and expensive. This platform automates that process with computer vision + AI so a similar product can be sold at consumer scale and price point.
- **BC-003** [Client-stated] The commercial model is a paywalled report: a user completes onboarding and photo upload, an AI-generated report is produced, and the user must pay to unlock/view the full report.
- **BC-004** [Client-stated] Project Sponsor: Jay Michaels (paying client). Delivery team: Crest Infosystems (Tejash Patel — PM, Jainesh Bhatt — Business Manager).
- **BC-005** [Client-stated] The client wants to be able to keep extending this codebase with their own team after delivery, using AI-assisted ("vibe coding") development tools — this drives a preference for clean, modular, well-documented code over clever/opaque implementations.
- **BC-006** [Client-stated] The project is explicitly being delivered in phases. Phase 1 is the current, scoped build; several features are deliberately deferred to a later phase (see Section 2 and Section 12).

---

## 2. Project Scope

### 2.1 Phase 1 — build now
- Landing page (marketing entry point).
- Custom backend-controlled user authentication: email + password + OTP, for both signup and login (see Section 5 — this replaces the earlier plan to use Supabase Auth directly).
- Onboarding questionnaire (23 questions, branching logic, ends in a mandatory disclaimer).
- Photo upload with guideline checklist UI **and** enforced backend validation (exact thresholds decided during development, see ASM-002).
- AI facial analysis engine (MediaPipe + OpenCV for measurement, OpenAI for narrative), built from scratch.
- Report generation (11-feature structured report, PDF export).
- Auto-publish of the generated report (no manual review gate in Phase 1).
- Payment (Stripe, one-time payment per report) gating full report access. Exact price still open (OQ-002).
- User dashboard (report history/status, download, payment history, profile).

### 2.2 Phase 2 — explicitly deferred, do not build now
- Admin panel and the full report status workflow: Draft → Pending Review → Approved → Published.
- Email notifications (SendGrid).
- PayPal as a second payment method.
- Meta Pixel + Google Tag Manager tracking.
- AI Visual Features: 5 AI-generated hairstyle variations, 5 AI-generated outfit variations, 3-image aging simulation (Now / +10 yrs / +20 yrs).
- AI Beauty Assistant — a ChatGPT-based chat where the user asks questions about their own report.
- Formal data-retention/privacy policy definition.

---

## 3. Actors & Roles (`BC-*` / `AUTH-*`)

- **End User** — signs up/logs in, completes onboarding, uploads photos, pays, views/downloads their own report(s). Phase 1's only functional role.
- **Admin** — Phase 2 only. Will review, edit, verify, and publish AI-generated reports, and manage recommendation content. Not built in Phase 1, but the data model must not preclude adding this role later (see AUTH-009).
- **Project Sponsor** — Jay Michaels; the business decision-maker for scope and requirements.
- **Delivery team** — Crest Infosystems.
- **System/third-party actors** — OpenAI (Vision/GPT for narrative analysis), MediaPipe/OpenCV (local CV libraries, not third-party services), Stripe (payments), a PostgreSQL database (hosting provider not yet decided, see `NFR-010`; **not** Supabase as of v1.2, and never used for authentication regardless of hosting provider — see Section 5), an email-based OTP delivery provider (channel confirmed as email; specific provider e.g. SendGrid/SES/Postmark still to be chosen during development).

---

## 4. Functional Requirements (`FR-*`)

| ID | Requirement | Source |
|---|---|---|
| FR-001 | Landing page with a clear call-to-action to begin onboarding. **Revised (v1.13):** added a header "Sign in" link (for a returning visitor, distinct from the "Get started" CTA) and an animated, scroll-triggered "How it works" section walking through the actual product flow (questionnaire → photos → analysis → report), per direct user request — `frontend/src/components/landing/LandingPage.tsx`. Structure/copy remain a `[Recommendation]`; no client content exists for this page (`CON-003`). | Client-stated |
| FR-002 | User account creation and login (see Section 5 for the full authentication spec). | Client-stated |
| FR-003 | 23-question onboarding questionnaire with branching logic, covering medical conditions/medications, self-perceived best feature, comfort with specific recommendation types (e.g. weight loss), frequency of appearance-related thoughts, and similar lifestyle/self-perception questions. **The literal 23 questions and branching logic are specified in [`docs/onboarding_questionnaire_spec.md`](./onboarding_questionnaire_spec.md)** (v1.6 — client-sourced from the Qoves Onboarding.mp4 reference recording, not delivery-team-invented; two small items in that spec remain `[Assumption]` pending client confirmation). | Client-stated (from Qoves onboarding reference) |
| FR-004 | The questionnaire must end with a mandatory, checkbox-gated disclaimer: user confirms no Body Dysmorphic Disorder–related concerns and understands recommendations are informational only, not medical guidance. The questionnaire cannot be submitted without this being checked. Exact wording in `docs/onboarding_questionnaire_spec.md` §4. | Client-stated (from Qoves onboarding reference) |
| FR-005 | Photo upload supporting multiple angles, presented alongside a 7-point guideline checklist: remove glasses/hat; use natural, even lighting; use a plain white background; tie back long hair; remove makeup; avoid neck-covering clothing; do not use filters. **The implemented flow (angle set, requirements screen, per-angle upload/camera capture) is specified in [`docs/photo_capture_spec.md`](./photo_capture_spec.md)** (v1.7 — delivery-team-authored, derived from the same Qoves reference material as the questionnaire; see `ASM-005`). | Client-stated (from Qoves onboarding reference) |
| FR-006 | Photo validation must be **enforced**, not just displayed as a checklist — see BR-005 and ASM-002 for the enforcement approach. **Resolved (v1.7):** implemented as six checks (file readability, resolution, brightness, face count, frame proportion, occlusion), thresholds recorded in `docs/security.md` §6. Plus a seventh, `pose_match` (angle-vs-photo agreement) and, as of v1.15, a set-level `identity_check` (cross-photo "same person" — see `ASM-010`). | Client-stated |
| FR-007 | Facial landmark detection and mathematical measurement performed via MediaPipe + OpenCV, built new (no existing engine to reuse). **Resolved (v1.8):** implemented in `Backend/app/services/facial_measurement_service.py` via MediaPipe's Face Landmarker (478-point mesh); 7 of the 11 report features (FR-009) get real geometry, the remaining 4 (Hair, Skin, Ears, Neck) get simpler heuristics or none at all — see `docs/database-design.md` §2.6. **Revised (v1.15):** the user reported the analysis appeared to use the front photo only. Confirmed accurate for one feature, Ears — a front-facing photo shows ears poorly at best, so it now measures each ear from its own left_3q/right_3q photo instead (falling back to front only if a side angle is missing). The other 7 mesh-based features stay front-only **by design, not oversight**: they're mostly left/right symmetry comparisons, and a 3/4-turned photo foreshortens whichever side is turned away, which would bias a symmetry measurement rather than improve it (verified empirically against this project's own turned-photo fixtures). See `ASM-010`. | Client-stated |
| FR-008 | AI-generated narrative explanations and personalized recommendations produced via OpenAI (Vision/GPT), using both the CV measurements **and** the questionnaire answers as context — not photo analysis alone. **Resolved (v1.8):** implemented against **DeepSeek**, not OpenAI (`ASM-006`), multimodal (photos included, not just measurements) — see `Backend/app/services/ai_narrative_service.py`. | Client-stated |
| FR-009 | Generated report must cover exactly 11 facial features: Hair, Eyebrows, Eyes, Nose, Cheeks, Jaw, Lips, Chin, Skin, Neck, Ears. **Resolved (v1.9):** implemented in `Backend/app/services/report_assembly_service.py`, always all 11 keys — see `docs/database-design.md` §2.7. | Client-stated (from Qoves sample report) |
| FR-010 | Each feature section includes: a narrative analysis, a before/after or "projected potential" framing, and a short summary callout. **Resolved (v1.9):** "projected potential" is textual only (relabeled `recommendation_ideas`), not a visual before/after — no AI-image generation exists in Phase 1 (that's Phase 2 scope). | Client-stated (from Qoves sample report) |
| FR-011 | The report as a whole includes an introduction, an "Understanding Your Results" preamble, an explicit limitations/disclaimer section, and a closing recommendations section synthesizing all findings. **Resolved (v1.9):** intro/preamble/limitations are static branded template copy (not AI-generated, since they're structural/standardized, not personalized); the closing recommendations section reuses Phase 4's own AI-synthesized `closing_recommendations` text. | Client-stated (from Qoves sample report) |
| FR-012 | Recommendations must span at-home/lifestyle, OTC/skincare-active, and optional in-clinic tiers, always framed as informational and never prescriptive, with "consult a qualified professional" language. **Resolved (v1.9):** tiers are produced by a keyword heuristic (`ASM-007`, not yet client-confirmed) over Phase 4's existing recommendation text — see `docs/database-design.md` §2.7. | Client-stated (from Qoves sample report) |
| FR-013 | Report is exportable as a downloadable PDF, branded with the team's own in-house design (no client-supplied branding assets exist yet). **Resolved (v1.9):** built with ReportLab (`Backend/app/services/report_pdf_service.py`), generated lazily on first download and cached. | Client-stated |
| FR-014 | In Phase 1, the generated report auto-publishes and is immediately available to the user — no manual admin review/approval step exists yet. **Resolved (v1.9):** `Report.publish_state` is set to `"published"` at creation time (`BR-002`). | Client-stated |
| FR-015 | The full report is inaccessible until the user completes a successful payment (Stripe). A teaser/results page may be shown pre-payment. **Resolved (v1.11):** implemented via Stripe hosted Checkout + webhook (`Backend/app/services/payment_service.py`) — per the user's direct v1.11 instruction, the gate sits earlier than the requirement strictly demands: payment blocks analysis from starting at all (`analysis_service.trigger_analysis`'s `PaymentRequiredError`), not just full-report reads, so there is no teaser to gate separately — see `docs/database-design.md` §2.8. | Client-stated |
| FR-016 | One-time payment per report is the recommended model (no subscription, no PayPal in Phase 1). **Resolved (v1.11):** `stripe.checkout.Session.create(mode="payment")`, price read from `REPORT_PRICE_CENTS`/`REPORT_PRICE_CURRENCY` config, never hardcoded. | Recommendation — price point explicitly still open, see OQ-002 |
| FR-017 | User dashboard showing report history/status, report download, payment history, and basic profile management. **Resolved (v1.12, revised v1.13):** implemented as a single sectioned `/dashboard` page composing `GET /users/me`, `GET /reports`, `GET /payments` (`docs/api-specification.md` §3/§7/§8/§9), plus a persistent "Dashboard" nav button in the app header. Profile management shows a `full_name` captured at signup and supports an in-app password change (`POST /auth/change-password`, no sign-out) — see `ASM-009`'s v1.13 revision. | Client-stated |

---

## 5. Authentication & Authorization Requirements (`AUTH-*` / `FE-*`)

This section reflects a **direct client decision that overrides the earlier recommendation to use Supabase Auth**. As of v1.2, Supabase is no longer part of the stack in any role (see `NFR-002`) — the constraint below is generalized to any third-party auth-as-a-service, not specific to Supabase. As of v1.1, the authentication flow itself is fully specified (see WF-002).

### 5.1 Backend authentication requirements
| ID | Requirement | Source |
|---|---|---|
| AUTH-001 | Authentication must be a custom, backend-controlled flow. Do **not** use Supabase Auth, Auth0, Firebase Auth, or any third-party auth-as-a-service as the application's direct authentication mechanism. | Client-stated |
| AUTH-002 | JWT-based authentication using an access token + refresh token pair. | Client-stated |
| AUTH-003 | Both **signup** and **login** follow the same two-step shape: (1) email + password submitted first; (2) once that step succeeds, an OTP is sent and must be verified; only after OTP verification does signup/login complete and tokens get issued. OTP is a mandatory second step in both flows, not an alternative to the password step. | Client-stated (confirmed v1.1) — see WF-002 for the full flow |
| AUTH-004 | Secure token generation, validation, refresh, and expiration handling, fully owned by the backend. | Client-stated |
| AUTH-005 | Backend APIs are solely responsible for authentication and authorization — no delegation to a third-party identity provider. | Client-stated |
| AUTH-006 | The system must properly handle: login, OTP verification, logout, token refresh, and session expiry as distinct, well-defined flows. | Client-stated |
| AUTH-007 | Protected API routes/endpoints must validate the JWT access token before allowing access. | Client-stated |
| AUTH-008 | Role/permission handling must exist where required. | Client-stated |
| AUTH-009 | The user data model must carry a `role` field (Phase 1: only `"user"` populated) so an `"admin"` role can be introduced in Phase 2 without a schema rework. | Recommendation, consistent with the Phase 2 admin-panel plan (Section 2.2) |
| AUTH-010 | Refresh tokens must be revocable server-side (a store of issued/rotated refresh tokens, keyed by a token identifier, not the raw token) so logout and session-expiry actually invalidate a session, not just let a client discard a token it still holds. | Decided by delivery team, client instruction: "decide by yourself considering security" (resolves former OQ-003, in part) |
| AUTH-011 | OTP security parameters: **10-minute expiry**, **60-second resend cooldown**, **5 failed attempts** before a **15-minute lockout**, OTP value stored **hashed**, never in plaintext. | Decided by delivery team, client instruction: "decide by yourself considering security" (resolves former OQ-003) |
| AUTH-012 | Token lifetimes: **access token — 15 minutes**; **refresh token — 7 days** (v1.4; was 30 days under the prior Zustand-held refresh model), rotated on every use, with reuse detection (if an already-rotated-out refresh token is presented again, the entire token family is revoked and the user is forced to re-authenticate, since this indicates possible theft). | Decided by delivery team, client instruction: "decide by yourself considering security" (resolves former OQ-003); lifetime revised in v1.4 |
| AUTH-013 | Cookie-authenticated endpoints (`/auth/refresh`, `/auth/logout`) require CSRF defenses — an `X-Requested-With` header plus an Origin/Referer allow-list — since they rely on an ambient httpOnly cookie credential rather than a bearer token the page must explicitly attach. | Decided by delivery team (not client-stated; implemented in the initial auth build, undocumented here until v1.5) |
| AUTH-014 | `GET /auth/me` returns the current authenticated user, used to rehydrate the frontend after a page reload once the refresh cookie has been exchanged for a fresh access token. | Decided by delivery team (not client-stated; implemented in the initial auth build, undocumented here until v1.5) |
| AUTH-015 | Forgot-password / reset-password: a user who forgets their password requests a one-time 6-digit email code (reusing `AUTH-011`'s OTP infrastructure, cooldown, and lockout), verifies it, then sets a new password. Verification and password-setting are two separate steps/screens, linked by a short-lived (~10-minute), single-use `reset_token` credential — never the raw OTP or email — issued only after OTP verification succeeds and rejected outright if reused, expired, or presented as a Bearer access token. On success, every existing session for the account is revoked (all devices), matching the same-account session-wipe behavior `AUTH-010` already requires elsewhere. See `docs/authentication.md` §2.5 for the full flow and `docs/api-specification.md` for endpoint contracts. | Decided by delivery team (not client-stated; `WF-002` never described a forgot-password path; the feature existed in shipped code since the initial auth build, undocumented here until v1.5; the three-screen/`reset_token` shape was decided in this v1.5 revision) |

### 5.2 Frontend token management requirements
| ID | Requirement | Source |
|---|---|---|
| FE-001 | Authentication state managed via **Zustand** (superseded in v1.2 — was Redux / Redux Toolkit). | Client-stated (v1.2) |
| FE-002 | **(v1.4)** The authenticated user's **access token** and user profile live in the Zustand auth store (memory only). The **refresh token** is stored exclusively as an **httpOnly cookie** set/rotated/cleared by the backend — not in Zustand, not in `localStorage`, not in `sessionStorage`. Zustand auth state must never use `persist` middleware. Session survival across page refresh / tab close / browser restart comes from the refresh cookie + startup `POST /auth/refresh` restore. Explicitly still **not** `localStorage`. See ASM-001. | Client-stated storage location revised in v1.4 (delivery team) for secure persistent sessions; supersedes v1.1–v1.3 "both tokens in Zustand" reading of OQ-009 |
| FE-003 | Authentication state and token handling must be centralized (a single auth store/service), not duplicated across components. | Client-stated |
| FE-004 | The access token must be automatically attached to authenticated API requests (e.g., via a centralized API client/interceptor). | Client-stated |
| FE-005 | Token expiration must be handled automatically using the refresh-token flow — proactive refresh ~1 minute before access-token expiry, plus 401→refresh→retry as fallback, transparent to the UI. Access-token expiry alone must not clear auth or redirect. Concurrent refresh attempts must share a single in-flight refresh (single-flight). | Client-stated; proactive refresh + single-flight clarified in v1.4 |
| FE-006 | When the **refresh session** becomes invalid (refresh returns 401: expired, revoked, missing, or reuse-detected), **or** the user explicitly logs out, the app must clear authentication state and redirect to login. Do **not** clear auth for access-token expiry, page refresh, tab/browser close, network errors, or 5xx. | Client-stated; invalid-refresh vs transient-error distinction clarified in v1.4 |
| FE-007 | Authentication logic must be kept separate from UI components — UI components should never talk to auth APIs or handle tokens directly. | Client-stated |

### 5.3 Required architectural separation
The client has specified this exact layering, and it must be preserved in any derived architecture/technical-design document:

```
UI → Zustand Auth Store → API Client → Backend Auth APIs → JWT/OTP Services
```

(Superseded in v1.2 — the store layer was previously named "Redux Auth Store"; the contract itself — single source of truth, no direct UI-to-API/token access, centralized API client — is unchanged, only the library changed from Redux to Zustand.)

- **UI** — presentational components; no direct API or token access.
- **Zustand Auth Store** — single source of truth for auth state (user, access token, auth status / initializing flag) on the frontend. Refresh token is **not** stored here (`FE-002` v1.4).
- **API Client** — centralized HTTP client that attaches the access token, sends cookies with `credentials: "include"`, and handles proactive refresh + refresh-on-401 (single-flight).
- **Backend Auth APIs** — FastAPI endpoints (Python) for register (email+password → OTP), login (email+password → OTP), OTP verify, refresh, logout, `/auth/me`, and protected-route dependencies for JWT validation. Sets/rotates/clears the httpOnly refresh cookie. (v1.2 had briefly specified Next.js Route Handlers under the since-rejected single-app consolidation; v1.3 reverts this to FastAPI — see change log.) The frontend (Next.js) and backend (FastAPI) are separate applications communicating over HTTP; the frontend calls these endpoints directly from the browser.
- **JWT/OTP Services** — backend-internal services for token issuance/validation and OTP generation/verification (email delivery), decoupled from the route handlers themselves.

---

## 6. Non-Functional / Technical Expectations (`NFR-*`)

| ID | Requirement | Source |
|---|---|---|
| NFR-001 | Backend: **Python + FastAPI** (v1.2 had briefly changed this to TypeScript + Next.js under the single-app consolidation reading; v1.3 reverts to Python + FastAPI now that the client confirmed two separate apps — see `ASM-004`, resolved). | Client-stated (v1.1, reconfirmed v1.3) |
| NFR-002 | Database: **PostgreSQL**, vendor-neutral (superseded in v1.2 — was Supabase-hosted Postgres specifically; Supabase is no longer part of the stack in any role, including as auth — see `CON-002`). Hosting provider for the Postgres instance remains undecided, consistent with `NFR-010`. Unaffected by v1.3. | Client-stated (v1.2) |
| NFR-003 | Database migrations: **Alembic** (v1.2 had briefly changed this to Prisma Migrate as a consequence of the since-reverted ORM/backend-language change; v1.3 reverts to Alembic along with `NFR-001`/`NFR-004`). | Client-stated (v1.1, reconfirmed v1.3), consistent with `NFR-004` |
| NFR-004 | ORM: **SQLAlchemy** (v1.2 had briefly changed this to Prisma because Prisma requires a JS/TS runtime; v1.3 reverts to SQLAlchemy now that the backend is confirmed to stay Python — see `NFR-001`). | Client-stated (v1.1, reconfirmed v1.3), consequence of `NFR-001` |
| NFR-005 | Frontend framework: **Next.js** (confirmed v1.1 as a separate app; v1.2 had briefly reframed this as consolidated with the backend into one app — v1.3 reverts that framing: Next.js frontend and FastAPI backend are two separate applications, per `ASM-004` resolved). | Client-stated (v1.1, re-scoped v1.3) |
| NFR-006 | Frontend state management: **Zustand**, at minimum for authentication state (Section 5.2) (superseded in v1.2 — was Redux / Redux Toolkit). Client instruction: use "another [library] for store management according to Next.js." Unaffected by v1.3. | Client-stated (v1.2) |
| NFR-007 | Facial analysis engine: MediaPipe + OpenCV, built from scratch. | Client-stated |
| NFR-008 | AI narrative/recommendation generation: OpenAI (Vision + GPT). **Implemented against DeepSeek instead — see `ASM-006`, not yet client-confirmed.** | Client-stated |
| NFR-009 | Payments: Stripe in Phase 1; PayPal deferred to Phase 2. | Client-stated |
| NFR-010 | Hosting: explicitly **not decided right now** — the client confirmed there is no current need to settle on a hosting platform. The earlier assumption of Replit (from the original pre-negotiation materials) is no longer treated as active; hosting is an open item to revisit later, not a blocker today. | Client-confirmed (v1.1, resolves former OQ-007 by deferring it) |
| NFR-011 | Code must be clean, modular, and well-documented to support the client's own team continuing development post-handoff. | Client-stated |
| NFR-012 | OTP delivery channel: **email**, confirmed. Specific provider (e.g. SendGrid, AWS SES, Postmark) still to be picked during development — not a blocking decision. | Client-confirmed channel (v1.1, resolves former OQ-004); provider selection delegated to dev team |

---

## 7. Business Rules (`BR-*`)

- **BR-001** [Client-stated] The full report is inaccessible until payment succeeds.
- **BR-002** [Client-stated] In Phase 1, a generated report auto-publishes immediately — there is no Draft/Pending Review/Approved gate. That workflow returns in Phase 2 once the Admin panel exists.
- **BR-003** [Client-stated] The BDD/informational-only disclaimer checkbox is a hard, non-optional gate before questionnaire submission.
- **BR-004** [Recommendation] Because Phase 1 has no admin safety net before publishing, the AI pipeline should not run — and no report should be generated — from photos that fail validation (BR-005), to avoid auto-publishing a low-quality report straight to a paying user.
- **BR-005** [Client-stated + delegated mechanism] Photo validation must be enforced by the backend, not left as a self-attestation checklist. Recommended checks: exactly one face detected; face occupies a reasonable proportion of the frame; no obvious occlusion over eyes/mouth (glasses/hat); minimum resolution; basic brightness/exposure check. The client confirmed exact thresholds should be decided during development (ASM-002), not specified up front. **Extended (v1.15, `ASM-010`):** a set-level cross-photo identity check — the recommended checks above are all per-photo; nothing previously caught a different person's photo being uploaded for one required angle, reported directly by the user.
- **BR-006** [Client-stated] Third-party usage costs (OpenAI, Stripe, the PostgreSQL hosting provider once chosen, and the email/OTP delivery provider) are the client's own responsibility, not included in any development estimate. (Supabase-specific wording removed in v1.2 — see `NFR-002`.)
- **BR-007** [Client-stated] No committed timeline or re-estimate is being produced at this stage; Phase 1 is scoped by feature list (Sections 2 and 4), not by hours or dollars, until the client asks otherwise.
- **BR-008** [Client-stated] Report content must reflect exactly 11 features (see FR-009) — this is a fixed structural rule, not a suggestion, derived directly from the reference Qoves sample report the client benchmarked their own draft report against.
- **BR-009** [Decided by delivery team, v1.5] Every user-initiated action with a real success/failure outcome must give visible feedback via a toast notification (top-right), never fail or succeed silently. Applies app-wide, not just to the auth module built so far. See `docs/ui-ux-design.md` for the full convention and its documented exceptions (e.g. anti-enumeration flows that must not reveal outcome).
- **BR-010** [Decided by delivery team, v1.5] Every destructive or irreversible action (delete, logout, revoke, etc.) must be confirmed via a shared confirmation dialog before it executes — never fire directly on click/tap. Applies app-wide; the auth module's logout is the first implementation, establishing the shared component every later module reuses.

---

## 8. Workflows (`WF-*`)

### WF-001 — End-to-end user journey (Phase 1)
1. Landing page → sign up / log in.
2. Onboarding: 23-question branching questionnaire → BDD/informational-only disclaimer checkbox → submit.
3. Photo Requirements confirmation screen (7-point checklist shown to user).
4. Photo upload (multi-angle) → backend validation (BR-005) → accept or reject with reason.
5. AI processing: MediaPipe/OpenCV landmark & measurement extraction → OpenAI narrative generation using measurements + questionnaire context.
6. Report assembled and auto-published (BR-002).
7. Results page shown with a teaser; full report locked (BR-001).
8. Stripe payment → on success, full report unlocked.
9. Dashboard: report history/status, PDF download, payment history, profile.

### WF-002 — Authentication flow (confirmed, v1.1)
Both **signup** and **login** follow the identical two-step shape:

1. **Step 1 — Email + password.**
   - *Signup:* user submits email + password → backend validates the email isn't already registered, hashes the password, creates a pending/unverified user record.
   - *Login:* user submits email + password → backend validates the credentials against the stored hash.
   - In both cases, on success the backend does **not** yet issue tokens — it proceeds to Step 2.
2. **Step 2 — OTP verification.** Backend generates an OTP, stores it hashed with an expiry (AUTH-011), and emails it to the user (NFR-012). The user submits the OTP.
   - Backend validates the submitted OTP against the stored hash and expiry, and against the attempt/lockout limits in AUTH-011.
   - On success: for signup, the account is marked verified; for login, the session is confirmed. Either way, the backend now issues a JWT access token + refresh token pair (AUTH-002, AUTH-012) and the flow completes as a successful signup or login.
   - On failure: an attempt counter increments; after 5 failed attempts, a 15-minute lockout applies (AUTH-011) before another OTP can be requested.
3. **Authenticated requests:** frontend API client attaches the access token to each request (FE-004); backend validates the JWT on protected routes (AUTH-007).
4. **Token refresh:** when the access token is near expiry (proactive) or an authenticated request returns 401 (reactive), the frontend calls `POST /auth/refresh` with the httpOnly refresh cookie (`credentials: "include"`) → backend validates it against its revocation store (AUTH-010), issues a new access token and rotates the refresh cookie → frontend updates the Zustand auth store transparently (FE-005). Access-token expiry alone must not clear auth or redirect. If the presented refresh token was already rotated out (reuse), the whole token family is revoked and the user must log in again.
5. **Logout:** explicit user action → frontend calls logout → backend revokes the refresh session and clears the cookie (AUTH-010) → frontend clears the Zustand auth store and redirects to login (FE-006). Do not clear auth from unload/visibility/route-change lifecycle hooks.
6. **Invalid refresh session:** if a refresh attempt fails with a confirmed auth error (401: expired, revoked, missing, or reused/rotated-out refresh token), the frontend clears the Zustand auth store and redirects to login (FE-006). Network errors and 5xx must not clear auth.

---

## 9. Data Entities (`DATA-*`, high-level — full schema belongs in `DATABASE_DESIGN.md`)

- **DATA-001 User** — email, hashed password, `full_name` (added v1.13, nullable — see `ASM-009`'s revision), verification status, `role` (AUTH-009), timestamps.
- **DATA-002 OTP Record** — linked to a user (or a pending signup), hashed OTP value, purpose (signup/login/password_reset — v1.5, see `AUTH-015`), expiry, attempt count, consumed flag.
- **DATA-003 Refresh Token / Session Record** — token identifier (not the raw token), user reference, issued/expiry timestamps, revoked flag, rotation lineage (for reuse detection per AUTH-012).
- **DATA-004 Questionnaire Response** — the 23 branching answers, linked to a user.
- **DATA-005 Photo** — per-angle uploads, validation status/results (BR-005).
- **DATA-006 Facial Analysis Result** — per-feature landmark/measurement output from MediaPipe/OpenCV, linked to a photo set.
- **DATA-007 Report** — the 11 feature sections (FR-009), generated PDF artifact reference, publish state (Phase 1: published-on-generation per BR-002), linked to a user, questionnaire response, and analysis result.
- **DATA-008 Payment** — Stripe payment/session identifier, status, amount, linked user/report.
- **Deferred to Phase 2:** Admin/reviewer accounts, report review history, AI-chat Conversation records, generated Visual Assets (hairstyle/outfit/aging images).

---

## 10. Constraints (`CON-*`)

- **CON-001** [Client-stated] No existing codebase exists — this is a from-scratch build; nothing from the original pre-negotiation "existing MVP" claims applies.
- **CON-002** [Client-stated, generalized in v1.2] No third-party authentication-as-a-service (e.g. Supabase Auth, Auth0, Firebase Auth) may be used as the application's authentication mechanism. (Prior to v1.2 this was worded Supabase-specifically, since Supabase was the database vendor at the time; Supabase is no longer part of the stack in any role as of v1.2 — see `NFR-002` — but the underlying rule is unchanged and generalizes to any such vendor.)
- **CON-003** [Client-stated] No client-supplied branding/design assets exist yet for Phase 1 — the team owns UI/branding decisions for now.
- **CON-004** [Client-stated] Third-party service costs (OpenAI, Stripe, the PostgreSQL hosting provider once chosen, email/OTP provider) are billed to and owned by the client, not bundled into any dev estimate. (Supabase-specific wording removed in v1.2 — see `NFR-002`.)
- **CON-005** [Client-stated] No committed delivery timeline or dollar estimate exists at this stage, and none has been requested.
- **CON-006** [Recommendation] Because Phase 1 auto-publishes reports with no admin safety net, the enforced photo-validation step (BR-005) is a harder requirement here than it would be in a workflow with human review downstream.
- **CON-007** [Client-stated] Hosting platform is explicitly undecided and not currently needed — do not assume Replit or any other specific host without a future decision.

---

## 11. Assumptions & Recommendations Not Yet Confirmed by the Client (`ASM-*`)

These are flagged separately per the documentation rules — they must stay visibly labeled as non-client-stated in any derived document until confirmed. As of v1.1, most former assumptions were resolved into confirmed requirements (see the Section 14 resolution table); the two below remain genuinely open or partially open.

- **ASM-001 — Token storage, residual XSS note (v1.4).** Refresh tokens are httpOnly cookies (not JS-readable). Only the short-lived access token lives in the in-memory Zustand store. This revises the earlier FE-002/OQ-009 decision that stored both tokens in the client-side store. Residual XSS exposure is limited to the in-memory access token for at most its 15-minute lifetime. Document in `SECURITY.md` with standard XSS hardening (CSP, output encoding, dependency hygiene) as the mitigation layer. Still forbidden: `localStorage`, `sessionStorage`, Zustand `persist` for auth.
- **ASM-002 — Photo validation rule set.** The specific automated checks in BR-005 are this document's recommendation; the client confirmed the exact thresholds should be finalized during development rather than specified now. Should be documented in `TECHNICAL_DESIGN.md`/`TESTING.md` once implemented.
- **ASM-003 — Payment model.** One-time payment per report (FR-016) is a recommendation; pricing itself remains explicitly open (OQ-002).
- **ASM-004 — Next.js consolidation (v1.2) — RESOLVED in v1.3.** The client's v1.2 instruction was "use typescript and nextjs in backend." The delivery team had initially interpreted this as consolidating the frontend and backend into one Next.js application. The client has since confirmed (v1.3) this reading was wrong: **the frontend and backend are two separate applications** — Next.js/TypeScript frontend, and the backend stays **Python + FastAPI** (not Next.js at all). See `NFR-001`, `NFR-003`, `NFR-004` v1.3 updates. This entry is kept for history rather than deleted, per this document's own traceability practice.
- **ASM-005 — Photo capture angle set (v1.7) — NOT YET CONFIRMED by the client.** [`docs/photo_capture_spec.md`](./photo_capture_spec.md) §1 defaults Phase 1 to **3 photo angles** (front, left 3/4, right 3/4) rather than the full 7-pose set shown in the client's own Qoves reference video. This is enough for MediaPipe/OpenCV to derive the core measurements `FR-007` needs, and reduces upload friction, but it is a scope reduction from what the client's own reference product uses — flag for sign-off at the next client check-in, alongside `OQ-002`. Implemented as a small, swappable constant (`REQUIRED_ANGLES`, `Backend/app/services/photo_validation_service.py`) so moving to 7 angles later is a config change, not a rearchitecture.
- **ASM-006 — AI vendor: DeepSeek, not OpenAI (v1.8) — NOT YET CONFIRMED by the client.** `NFR-008` names OpenAI specifically (Vision + GPT), not a delegated/open item the way the OTP-email vendor was. `facial-analysis-engine` is built against **DeepSeek** instead — its hosted API is OpenAI-Chat-Completions-compatible (same request/response shape, the `openai` SDK works against it via a `base_url` override), and the delivery team had a DeepSeek API key available for development. The narrative call is multimodal (the photos themselves are sent, not just derived measurements), matching `FR-008`'s "not photo analysis alone" / `NFR-008`'s literal "Vision" wording — DeepSeek shipped an experimental vision-capable model (`deepseek-v4-flash-vision-exp`) on the same endpoint. This is a genuine deviation from a client-stated vendor, not a delivery-team-open item — flag for sign-off at the next client check-in, alongside `ASM-005`/`OQ-002`. Built as a configuration swap, not a new implementation (`AI_BASE_URL`/`AI_API_KEY`/`AI_MODEL`, `Backend/app/services/ai_narrative_service.py`) — switching to real OpenAI later needs no code change.
- **ASM-007 — Recommendation tiering: keyword heuristic, not AI (v1.9) — NOT YET CONFIRMED by the client.** `FR-012` requires recommendations to span three tiers (at-home/lifestyle, OTC/skincare-active, in-clinic). Phase 4's existing AI output (`narrative_result`) only has a flat list of recommendation strings per feature, no tier labels. Rather than modifying Phase 4's already-implemented AI prompt or adding a second AI call (either would add cost/complexity or touch completed code), `report-generation` buckets the existing strings into the three tiers with a keyword classifier (`Backend/app/services/report_assembly_service.classify_recommendations` — "serum"/"cream" → OTC, "consider seeing a dermatologist"/"laser" → in-clinic, everything else → at-home). This is a first-pass, delivery-team judgment call, not a clinical classification — expect misclassifications and a future tuning pass, same posture as `ASM-002`'s photo-validation thresholds. Flag for sign-off at the next client check-in, alongside `ASM-005`/`ASM-006`/`OQ-002`.
- **ASM-008 — Pay-before-analysis gate (v1.11, revised from v1.10's deferred two-stage pipeline) — refinement, not a deviation.** `FR-008`'s AI narrative call originally ran in full at `POST /analysis` time, before payment exists at all. v1.10 first addressed this with a two-stage split (free CV measurements immediately, paid DeepSeek narrative deferred until payment). The user then gave a direct instruction overriding that hybrid: **payment must complete before analysis starts at all** — not even the free CV step runs pre-payment. `analysis_service.trigger_analysis` now requires a succeeded `Payment` before it will create a `FacialAnalysisResult` row at all (`PaymentRequiredError`, 402); the trigger itself stays a user-facing "Start Analysis" click (`POST /analysis`), same as before payment gating existed — the Stripe webhook only flips `Payment.status`, it does not run the pipeline itself (an earlier same-day version had it do so automatically; reverted per user feedback that this made the "analyzing" step invisible). `FR-015`'s "a teaser/results page **may** be shown pre-payment" is optional wording — not showing one is still compliant, just a narrower exercise of that option. `FR-008`'s combined-CV-plus-questionnaire-context narrative is honored exactly as stated, just entirely post-payment now. No client sign-off needed (no client-stated requirement text changes — `FR-015`/`BR-001` already permitted a model with no pre-payment content at all), but recorded here per this document's own decision-traceability practice. See `Backend/app/services/analysis_service.py` (`trigger_analysis`, `run_analysis_pipeline`), `Backend/app/services/payment_service.py` (`handle_webhook_event`), `docs/database-design.md` §2.6/§2.8, `docs/api-specification.md` §6/§8.
- **ASM-009 — Profile management is view-only (v1.12) — refinement, not a deviation. REVISED in v1.13 — see below.** `FR-017` asks for "basic profile management," but `DATA-001` (User) is explicitly scoped to email, hashed password, verification status, role, and timestamps — no name/display-name or other editable field exists anywhere in the schema. Rather than inventing a field to have something for `PATCH /users/me` to edit, profile management surfaces email, verification status, and member-since date (`GET /users/me`, new), plus a "Change password" action that signs the user out and redirects to the existing forgot-password OTP flow (`(auth)/layout.tsx` bounces any already-authenticated user away from `/forgot-password`, so this can't be a plain in-session link) rather than a new authenticated password-change endpoint. No client sign-off needed (`FR-017`'s text names no specific field), but recorded here per this document's own decision-traceability practice. Kept for history rather than deleted, per this document's own traceability practice (see `ASM-004`'s precedent) — **the two paragraphs below describe what is actually built now.**

  **v1.13 revision — the user gave two direct, explicit instructions overriding the above.** (1) *"in welcome back, instead of email while register take name from user"* — `full_name` was added to `DATA-001` (nullable, for pre-existing rows) and made required on `POST /auth/register`; the dashboard greeting and `UserMenu` now use it (falling back to the old email-derived guess only for accounts created before this field existed). This is a genuine, small schema addition — not "inventing" scope on the delivery team's own initiative, since the user directly asked for it — but it's still not independently editable after signup (no `PATCH /users/me`), so §3's endpoint table is otherwise unchanged. (2) *"for change password no need to signout user. just show old pass and new pass and confirm new pass input and on submit it changes"* — a new authenticated `POST /auth/change-password` (`{current_password, new_password}`) replaces the old sign-out-and-redirect-to-forgot-password flow. Deliberately does **not** revoke the caller's session afterward (unlike `POST /auth/reset-password`, which revokes every session) — the user is proving identity via the current password itself, not an emailed code, so the same "assume compromise, sign out everywhere" posture doesn't apply. See `docs/api-specification.md` §2/§3, `docs/database-design.md` §2.1, `Backend/app/services/user_service.py` (`change_password`), `Backend/app/api/routers/auth.py`, `frontend/src/components/dashboard/ProfileCard.tsx`.

- **ASM-010 — Cross-photo identity check: landmark-ratio signature + empirically calibrated threshold (v1.15) — first-pass heuristic, same posture as `ASM-002`.** The user reported that uploading a different person's photo for one required angle was silently accepted — `BR-005`'s recommended check list (exactly one face, frame proportion, occlusion, resolution, brightness) never included a cross-photo "is this the same person" comparison, only per-photo checks. No dedicated face-recognition/embedding model is used (would mean a new, heavier dependency — dlib's `face_recognition`, `deepface`, etc. — not attempted without being asked); instead, `photo_validation_service.compute_face_signature` derives 5 inter-landmark distance ratios (eye/nose/mouth/jaw/face width, all normalized by inter-ocular distance) from the same MediaPipe Face Landmarker `facial_measurement_service.py` already uses, and `check_photo_set_identity` flags whichever of the three uploaded photos has the largest total distance to the other two once all three individually pass their own checks. The 0.6 mismatch threshold was measured, not assumed: computed against this project's own front/right_3q/left_3q fixtures (same person; pairwise distances 0.08–0.33) and a second, genuinely different person's photo (`tests/fixtures/photos/different_person.jpg`, a public-domain US Navy portrait already bundled as ML sample data — different-person pairwise distances 0.71–0.93). Enforced in the UI (`PhotoSetCompleteStep.tsx` blocks Continue and highlights the flagged photo) and independently re-checked server-side at both `POST /payments/checkout` and `POST /analysis` (`PhotoIdentityMismatchError`) so it can't be bypassed by calling either endpoint directly. Not yet validated against a large, diverse real-world photo set — expect a tuning pass, same as every other threshold in `photo_validation_service.py`. See `docs/client_requirements.md`'s FR-007 entry below for the related Ears-measurement fix (same v1.15 change, different root cause). **v1.16 addendum:** the detection and per-photo retake flow above were correct from the start, but a separate routing gap let a mismatched-yet-individually-passed set bypass the `/photos` screen entirely from most entry points (e.g. straight after login) — see the v1.16 change-log entry above for the fix (`getNextOnboardingStep`'s `photosIdentityConsistent` parameter). **v1.17 addendum:** the odd-one-out *selection* itself had a real accuracy gap — largest-summed-distance could occasionally name the wrong photo — replaced with a deterministic match-count vote (front protected as reference on a tie); see the v1.17 change-log entry above. **v1.18 addendum:** the "all three mutually different" tie-break still picked only one of the two non-front photos (by distance-to-front, a pose-geometry artifact, not an identity signal) — now both non-front photos are returned together, and the API's `odd_one_out: string | null` field is replaced by `mismatched_angles: list[str]` throughout to represent that; see the v1.18 change-log entry above. **v1.19 addendum:** the user supplied a full written spec of the expected stateless-recompute behavior (retake → clear/replace, never blindly preserve a prior error); audited the implementation against it clause by clause and found it already conformant (no code change needed) — see `docs/photo_identity_validation_spec.md` for the full design contract and worked-example table, and the v1.19 change-log entry above for what was added to make that verifiable going forward. **v1.20 addendum:** the v1.19 audit was incomplete — it missed that `usePhotoUploadGuard.ts`'s own auto-redirect effect read the live `identityCheck` value (including its transient post-retake `null` state) as "safe to skip the review screen," which fired a premature redirect to `/payment` on nearly every retake, before the fresh comparison ever came back. Fixed by deciding that redirect once, from the initial page-load fetch only, never from live retake-driven state changes; see the v1.20 change-log entry above and `docs/photo_identity_validation_spec.md` §6 for the full root-cause writeup. **v1.21 addendum:** `PhotoSetCompleteStep.tsx` itself had separately reverted to the pre-v1.18 `odd_one_out` field name (a real `tsc` compile error, silently non-blocking in dev mode) — every per-photo error highlight was broken (all cards rendered green) even though the underlying comparison and message text were correct. Re-fixed, and live-verified end-to-end with three genuinely distinct synthesized identities that the exact "all three different → fix one → fix the other" sequence narrows the flagged set correctly at each step; see the v1.21 change-log entry above.

---

## 12. Out of Scope for Phase 1

Restated from Section 2.2 for clarity when this document is used to scope a sprint or derived PRD: Admin panel, full report status workflow (Draft/Pending Review/Approved/Published), email notifications, PayPal, Meta Pixel/GTM tracking, AI Visual Features (hairstyle/outfit/aging generation), AI Beauty Assistant chat, formal data-retention/privacy policy.

---

## 13. Traceability Map — how this document feeds derived documents

| Derived document | Primary requirement IDs to draw from |
|---|---|
| `PRD.md` | `FR-*`, `WF-001`, Section 2 (scope) |
| `BRD.md` | `BC-*`, `BR-*`, Section 2 (scope), `CON-*` |
| `ARCHITECTURE.md` | `NFR-*`, Section 5.3 (layering), `WF-002` |
| `TECHNICAL_DESIGN.md` | `NFR-*`, `AUTH-*`, `FE-*`, `ASM-*` |
| `API_SPECIFICATION.md` | `AUTH-001`–`AUTH-015`, `WF-002` (endpoint sequence), `FR-*` (endpoints implied by each functional requirement) |
| `DATABASE_DESIGN.md` | `DATA-*` |
| `UI_UX_REQUIREMENTS.md` | `FR-003`–`FR-005`, `FE-*`, Qoves reference materials |
| `AUTHENTICATION.md` | `AUTH-*`, `FE-*`, `WF-002`, `ASM-001` |
| `SECURITY.md` | `AUTH-010`–`AUTH-015`, `ASM-001`, `BR-005`/`CON-006` |
| `TESTING.md` | Every `FR-*`/`AUTH-*`/`BR-*` should map to at least one test case when this doc is written |
| `DEPLOYMENT.md` | `NFR-010`/`CON-007` (hosting, currently undecided), `CON-002` (no third-party auth-as-a-service), `NFR-001`–`NFR-003` |

Any derived document should cite requirement IDs from this table so a later change here can be traced forward to what it affects.

---

## 14. Open Questions (`OQ-*`)

### Resolved in v1.1
| ID | Original question | Resolution |
|---|---|---|
| OQ-001 | Login mechanism shape (password vs. OTP-only vs. both) | **Resolved** — both signup and login use email + password first, then a mandatory OTP verification step, then completion. See AUTH-003, WF-002. |
| OQ-003 | Exact token/OTP lifetimes and thresholds | **Resolved** — client delegated this to the delivery team "considering security"; concrete values set in AUTH-010/011/012. |
| OQ-004 | OTP delivery channel/provider | **Resolved (channel)** — email confirmed. Specific provider still to be picked during development (not blocking). See NFR-012. |
| OQ-005 | ORM confirmation | **Resolved (v1.1), briefly superseded (v1.2), reverted (v1.3)** — SQLAlchemy confirmed in v1.1, replaced by Prisma in v1.2 under the since-rejected single-app consolidation, reverted back to SQLAlchemy in v1.3. See NFR-004. |
| OQ-006 | Frontend framework | **Resolved (v1.1), re-scoped (v1.2), re-scoped again (v1.3)** — React + Vite confirmed in v1.1; v1.2 changed the frontend to Next.js and (incorrectly) framed it as consolidated with the backend; v1.3 keeps Next.js but confirms it as a **separate** app from the (Python/FastAPI) backend. See NFR-005, ASM-004 (resolved). |
| OQ-007 | Hosting reconfirmation | **Resolved by deferral** — client confirmed no hosting decision is needed right now. See NFR-010, CON-007. |
| OQ-008 | Exact automated photo-validation thresholds | **Resolved (v1.7)** — implemented as a six-check gate, thresholds recorded in `docs/security.md` §6. See ASM-002. |
| OQ-009 | Refresh token storage (client-side store vs. `httpOnly` cookie) | **Resolved (v1.4)** — httpOnly cookie for the refresh token; access token only in Zustand memory. Earlier v1.1 client confirmation of "both tokens in client-side store" is superseded for the refresh token to support secure persistence across browser restarts without `localStorage`. See FE-002, ASM-001. |

### Still open
| ID | Question | Status |
|---|---|---|
| OQ-002 | Report price / pricing model specifics | **Explicitly kept open by the client** ("keep this open question currently"). FR-016/ASM-003 (one-time payment per report) stands as the working model; the price itself should remain a configurable value, not hardcoded, until this is resolved. |
| OQ-010 | Photo capture angle count — 3 angles (implemented default) vs. the client's own 7-pose Qoves reference | Not yet put to the client — **new as of v1.7**. See `ASM-005`, `docs/photo_capture_spec.md` §5. |
| OQ-011 | AI vendor — DeepSeek (implemented) vs. the client-stated OpenAI | Not yet put to the client — **new as of v1.8**. See `ASM-006`, `docs/database-design.md` §2.6. |
| OQ-012 | Recommendation-tiering approach — keyword heuristic (implemented) vs. an AI-based classification | Not yet put to the client — **new as of v1.9**. See `ASM-007`, `docs/database-design.md` §2.7. |

---

### Summary
This document is the authoritative, single source of truth for the AI Facial Analysis platform. Phase 1 delivers: a from-scratch build with custom backend-controlled authentication (email + password, then mandatory OTP verification, for both signup and login; forgot-password via a separate OTP-verify-then-set-new-password flow, `AUTH-015`; JWT access + refresh tokens with rotation and reuse detection; **httpOnly refresh cookie** + Zustand-held access token — explicitly not `localStorage`/`sessionStorage`; sessions persist across refresh/tab/browser restart until explicit logout or an invalid refresh session; and a strict UI → Zustand Auth Store → API Client → Backend Auth APIs → JWT/OTP Services layering), a 23-question onboarding flow, enforced photo validation (thresholds to be finalized during development), a MediaPipe/OpenCV + OpenAI facial analysis pipeline, an 11-feature auto-published report with PDF export, Stripe-gated payment, and a basic user dashboard — with app-wide toast feedback (`BR-009`) and confirm-before-destructive-action (`BR-010`) conventions established here and expected to carry into every later module — on **two separate applications**: a Next.js/TypeScript frontend and a Python/FastAPI + SQLAlchemy/Alembic backend, communicating over HTTP, both over a vendor-neutral PostgreSQL database. Admin functionality, the full report review workflow, email notifications, PayPal, Meta Pixel/GTM, AI visual features, and the AI Beauty Assistant are deferred to Phase 2. As of v1.9, report pricing (`OQ-002`) remains open at the client's explicit request, alongside three delivery-team implementation choices not yet put to the client: the photo-capture angle count (`ASM-005`/`OQ-010`), the AI vendor substitution, DeepSeek instead of the client-stated OpenAI (`ASM-006`/`OQ-011`), and the recommendation-tiering keyword heuristic (`ASM-007`/`OQ-012`). The frontend/backend architecture question (`ASM-004`), refresh-token transport (`OQ-009`), the onboarding-questionnaire content (`FR-003`/`FR-004`, see `docs/onboarding_questionnaire_spec.md`), photo-validation thresholds (`ASM-002`/`OQ-008`), and whether a user can have multiple reports (resolved: no, one per user — `docs/database-design.md` §5) are resolved; hosting remains deferred (not blocking) rather than open.
